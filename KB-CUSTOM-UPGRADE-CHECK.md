# KB Custom - Upgrade Risk Checklist

현재 벤더 기준 브랜치(`vendor/<현재버전>`) → 다음 버전으로 올릴 때, 진행 가능 여부
(가능/위험)를 판단하기 위한 체크리스트입니다. `KB-CUSTOM-MODIFIED.md` / `KB-CUSTOM-NEW.md`에
있는 파일 목록과 대조하며 아래 항목을 순서대로 확인하세요. 단순 merge conflict 여부만 보지
말고 이 파일의 항목을 모두 통과해야 업그레이드를 진행합니다.

업그레이드는 **한 번에 한 단계씩** 진행하며, 아래 점검 이력에 오래된 것부터(위→아래) 순서대로
기록합니다. **현재 기준점: `vendor/1.13.3`.**

점검 결과는 버전별로 별도 MD 파일을 만들지 말고 하단 **점검 이력**에만 기록합니다.
단, 하나라도 **위험**으로 판단되면 그때만 원인/영향 범위를 담은 별도 파일
(`KB-CUSTOM-UPGRADE-<버전>.md`)을 만들고 점검 이력의 비고에서 링크하세요.

## 체크리스트

### 1. 네이밍 충돌 (최우선)

우리가 커스텀으로 추가한 서비스/엔티티 타입명이 새 버전에서 공식으로 추가/변경되지 않았는지 확인.

- [ ] 새 버전 릴리즈 노트/CHANGELOG에서 우리 커스텀 커넥터/엔티티와 같은 이름이 언급되는지 검색
- [ ] `openmetadata-spec/.../databaseService.json`(또는 해당 서비스 타입 스키마)의 새 버전 enum
      목록에 우리 커스텀 타입명이 이미 존재하는지 확인
      (`git diff vendor/<현재버전> vendor/<신버전> -- <해당 스키마 파일>`)
- [ ] 공식 엔티티 타입 목록(`EntityType` enum, `openmetadata-spec/.../entity/data/*.json`)에
      우리 커스텀 엔티티와 이름이 겹치는 신규 엔티티가 추가되지 않았는지 확인
- 충돌 시: 공식 구현으로 교체하고 우리 커스텀 파일(`-kb-cust` 접미사 / Java `KbCust` 접미사)은 폐기

### 1-1. 대규모 리팩터링/API 개명 (네이밍 충돌은 아니지만 위험도가 높은 별도 항목)

단순 이름 충돌이 아니어도, 우리 코드가 참조하는 공식 코드의 **내부 구조 자체**가 바뀌면
(정적 매핑 → 함수 호출로 전환, enum 값 리네이밍 등) 다수 파일에 걸쳐 수작업 재통합이 필요할 수 있음.

- [ ] 우리가 확장한 핵심 파일(`ServiceUtilClassBase.ts`, `SearchClassBase.ts`,
      `EntityUtilClassBase.ts`, `search.enum.ts`, `search.interface.ts` 등)이 새 버전에서
      구조적으로(단순 필드 추가가 아니라 클래스/함수 시그니처, 데이터 구조 자체가) 바뀌었는지 확인
- [ ] 바뀌었다면 실제 cherry-pick 리허설(아래 6번 참고)로 충돌 성격이 "기계적 병합 가능"인지
      "재설계 필요"인지 먼저 판별

### 2. 생성 코드 재생성 영향

`KB-CUSTOM-MODIFIED.md`에 기록된 generated 파일들은 직접 수정한 것이라
`mvn generate` / `yarn parse-schema` 재실행 시 덮어써질 수 있음.

- [ ] 새 버전에서 관련 스키마의 enum 배열 구조(순서, 포맷)가 바뀌었는지 확인
- [ ] 새 버전으로 스키마 재생성 후 generated 파일에서 우리 커스텀 타입이 빠졌는지 확인 →
      빠졌다면 `KB-CUSTOM-MODIFIED.md`의 수정 패턴을 새 버전 코드에 맞게 재적용

### 3. Flyway 마이그레이션

기존 DB에 우리 커스텀 엔티티/서비스 타입의 row가 이미 존재하는 상태에서 업그레이드됨.

- [ ] 새 버전의 `bootstrap/sql/migrations/`에 관련 컬럼/테이블을 검증·정규화·백필하는
      마이그레이션이 추가됐는지 확인 (`git log --oneline vendor/<현재버전>..vendor/<신버전> -- bootstrap/sql/migrations`)
- [ ] 있다면 스테이징 DB로 먼저 마이그레이션 시도해서 커스텀 값에 대해 실패하지 않는지 확인

### 4. UI 스키마 리졸브 파이프라인 (`parseSchemas.js`)

- [ ] `openmetadata-ui/.../parseSchemas.js`가 새 버전에서 변경됐는지 확인
- [ ] 변경됐다면 `*-kb-cust.json` 커넥션 스키마들이 정상적으로 resolve되어
      `src/jsons/connectionSchemas/`에 출력되는지 재확인

### 5. 빌드 툴체인 변경

- [ ] `openmetadata-ui/pom.xml`의 node/yarn 버전, `package.json`의 vite/rollup 버전이
      바뀌었는지 확인
- [ ] 바뀌었다면 `KB-CUSTOM-TEST.md`에 기록된 Windows 이슈(ARM64 rollup, longpath,
      license-header-fix 등)가 재발하는지 재검증

### 6. 파일 단위 충돌 대조 (실제 리허설 권장)

- [ ] `KB-CUSTOM-MODIFIED.md`에 나열된 모든 파일에 대해
      `git diff vendor/<현재버전> vendor/<신버전> -- <파일>` 로 상위 diff 확인
- [ ] 충돌 없는 파일: 그대로 rebase/merge
- [ ] 충돌 파일: 수동 재적용 후 `KB-CUSTOM-MODIFIED.md`에 변경 사유 기록
- [ ] **가능하면 diff만 보지 말고 실제로 리허설할 것**: 임시 브랜치를 새 버전 릴리즈 태그에서
      만들어 `git cherry-pick vendor/<현재버전>..<현재 최신 커스텀 브랜치>`로 우리 커밋을
      전부 실제로 재생해보고, 충돌 성격(단순 tail-append vs 구조적 재설계 필요)을 직접 확인한 뒤
      **`git cherry-pick --abort`로 되돌리고 임시 브랜치는 삭제**. 리허설 단계에서 최소
      `mvn -pl openmetadata-service -am install`까지는 실제로 돌려서, `tsc --noEmit`만으로는
      안 보이는 백엔드 인터페이스 계약 변경 등을 놓치지 않을 것. 실제 반영은 이 리허설로
      가능 여부를 확정한 뒤에만 진행.

## 판단 기준 (가능/위험/가능-단 수작업 필요)

- 1번(네이밍 충돌)에 걸리면 **위험** — 업그레이드 전에 커스텀 커넥터/엔티티를 먼저 재설계해야 함
- 1-1번(구조적 리팩터링)에 걸리면 **가능(단 수작업 필요)** — 이름 충돌은 아니라 진행은 가능하지만,
  단순 병합으로 끝나지 않고 여러 파일을 우리가 직접 새 구조에 맞게 다시 작성해야 함. 예상 작업량이
  크면 실제 업그레이드 착수 전 사용자에게 먼저 보고하고 진행 여부를 확인할 것
- 2~6번은 걸려도 **가능** — 단, 반영 후 `KB-CUSTOM-TEST.md`에 재검증 결과를 남기고 진행
- 어느 항목이든, 리허설이 코드 빌드까지만 확인했다면 **배포(Docker 이미지/마이그레이션 상태)
  단계에서 별도 버그가 나올 수 있다는 점을 리포트에 명시할 것** — 리허설 통과가 배포 무결점을
  보장하지 않음

## 점검 이력

`사전 판단`은 실제 merge/cherry-pick 전에 이 체크리스트로 예측한 결과, `실제 결과`는
포팅을 실제로 수행한 뒤 확인된 결과입니다. 예측이 틀렸을 수도 있으니 두 값을
덮어쓰지 말고 히스토리로 남겨두세요.

| 날짜 | 대상 버전 | 결과 일치 여부 |
|---|---|---|
